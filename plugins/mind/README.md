# mind

`mind` will be a store of the owner's preferences and decisions, injected into every session at start so the owner never has to re-explain the same context twice. It is not built yet — this plugin is a placeholder reserving the name and version slot in the `toolshed` marketplace.
