# Gotchas

<!-- Add project-specific gotchas as you discover them. -->
<!-- These are non-obvious behaviors that cause bugs if you don't know about them. -->

<!-- Examples of good gotchas: -->
<!-- - **structlog + caplog**: caplog doesn't capture structlog output. Use capsys instead. -->
<!-- - **Decimal("0.00") is falsy**: any `if price:` or `price or default` pattern is a bug. -->
<!-- - **pydantic-settings JSON-parses set[str]**: use str | set[str] with model_validator. -->
<!-- - **Library X reads env vars directly**: pass config explicitly, don't rely on os.environ. -->

## TODO: Add your project's gotchas here
