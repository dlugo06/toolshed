{
  "phase": "phase0-example",
  "title": "Example Phase — Delete This",
  "description": "Demonstrates the PRD schema. Delete this file and create your own phases with /dev-kit:initialize-phase.",
  "items": [
    {
      "id": "P0-001",
      "category": "setup",
      "title": "Set up project structure",
      "description": "Create src/, tests/, and configuration files. This is the foundational task that establishes the project layout.",
      "depends_on": [],
      "passes": false,
      "steps_to_verify": [
        "src/ directory exists with __init__.py",
        "tests/ directory exists with conftest.py",
        "pytest runs and discovers 0 tests (no failures)"
      ]
    },
    {
      "id": "P0-002",
      "category": "data",
      "title": "Add first model and migration",
      "description": "Create the primary data model with DB migration. This establishes the data layer.",
      "depends_on": ["P0-001"],
      "passes": false,
      "steps_to_verify": [
        "Model class exists with correct fields",
        "Migration file exists and applies cleanly",
        "Unit test creates and queries the model"
      ]
    },
    {
      "id": "P0-003",
      "category": "core",
      "title": "Implement core business logic",
      "description": "The main feature that the project exists to deliver. This is where the value lives.",
      "depends_on": ["P0-002"],
      "passes": false,
      "steps_to_verify": [
        "Function accepts input and returns expected output",
        "Edge cases are tested (empty input, invalid data)",
        "Integration test covers the full path"
      ]
    }
  ]
}
